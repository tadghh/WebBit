<% content_for :primary_content do%>
	<div class="flex flex-col">
		<div class="w-6/12">
			<%= link_to "<- Back", submissions_path, class: "pt-3 pl-0 btn-xs bold btn" %>
		</div>
		<h1 class="text-4xl font-bold">Editing submission</h1>
	</div>

	<%= render "form", submission: @submission %>
<% end%>

<% content_for :secondary_content do%>
	<%= render "guidelines" %>
<% end%>

<%= render "basic_layout"%>
