<% content_for :community_label, @community.name%>

<div class="py-10 w-full bg-indigo-600">
	<%= @community.title%>
	<%= @community.name%>

</div>

<% content_for :primary_content do %>
	<% @community.submissions.each do |submission| %>
		<%=  render "submissions/submission", submission: submission %>

	<%end %>
<%end %>

<% content_for :secondary_content do%>
	<%=  render "sidebar", community: @community %>

<% end %>
<%=  render "basic_layout" %>
